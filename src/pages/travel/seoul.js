import React from 'react';
import TravelContainer from '../../components/travel/TravelContainer';
import Layout from '../../components/layout';
const Seoul = () => {
  return (
    <Layout>
      <TravelContainer>This is the Seoul Page!</TravelContainer>
    </Layout>
  );
};

export default Seoul;
